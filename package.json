{
  "name": "mega-dash",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test tests/*.test.js",
    "smoke": "node tools/smoke.mjs",
    "apk": "npm run build && npx cap sync android && cd android && ./gradlew assembleRelease",
    "status": "node tools/status.js",
    "sync": "node tools/sync-tracker.js"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "vite": "^5.4.8"
  }
}
